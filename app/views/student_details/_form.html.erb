<%= form_with(model: student_detail, local: true) do |form| %>
  <% if student_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_detail.errors.count, "error") %> prohibited this student_detail from being saved:</h2>

      <ul>
      <% student_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :budget %>
    <%= form.number_field :budget, id: :student_detail_budget %>
  </div>

  <div class="field">
    <%= form.label :enrolmentDate %>
    <%= form.date_select :enrolmentDate, id: :student_detail_enrolmentDate %>
  </div>

  <div class="field">
    <%= form.label :disrolmentDate %>
    <%= form.date_select :disrolmentDate, id: :student_detail_disrolmentDate %>
  </div>

  <div class="field">
    <%= form.label :graduationDate %>
    <%= form.date_select :graduationDate, id: :student_detail_graduationDate %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :student_detail_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
