<%= form_with(model: employee_detail, local: true) do |form| %>
  <% if employee_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_detail.errors.count, "error") %> prohibited this employee_detail from being saved:</h2>

      <ul>
      <% employee_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :salary %>
    <%= form.text_field :salary, id: :employee_detail_salary %>
  </div>

  <div class="field">
    <%= form.label :employmentDate %>
    <%= form.date_select :employmentDate, id: :employee_detail_employmentDate %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :employee_detail_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
