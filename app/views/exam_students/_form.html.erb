<%= form_with(model: exam_student, local: true) do |form| %>
  <% if exam_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam_student.errors.count, "error") %> prohibited this exam_student from being saved:</h2>

      <ul>
      <% exam_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :exam_id %>
    <%= form.text_field :exam_id, id: :exam_student_exam_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :exam_student_user_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :exam_student_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
